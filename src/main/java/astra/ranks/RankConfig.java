package astra.ranks;

public class RankConfig {
    public static enum Ranks {
        OWNER,
        ADMIN,
        SUPPORT,
        DEV,
        BUILDER,
        BOOSTER,
        ELITE,
        MEMBER
    }
}
